# Dependencies are installed by EAS from package-lock.json.
node_modules/

# Local Expo and generated verification artifacts are not app source.
.expo/
.expo-*/
.export-*/
.web-export-*/
dist-*/
NUL
*.log

# Gradle rebuilds these directories in the cloud.
android/.gradle/
android/app/build/
android/build/

# Local-only environment files are replaced by the preview profile variables.
.env
.env.*
